#include "monty.h"
/**
 * pop - Removes the top element of the stack.
 * @stack: Pointer to a pointer of the top element  of the stack.
 * @int: Integer representing the line number of of the opcode.
 */
void pop_top(stack_t **stack, unsigned int)
	{
	stack_t *tmp;

	if (stack == NULL || *stack == NULL)
	   printf("can't pop an empty stack\n");
       
	exit(EXIT_FAILURE)
	
}
